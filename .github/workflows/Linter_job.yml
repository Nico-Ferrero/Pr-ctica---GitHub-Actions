name: Main Workflow
on:
  push:
    branches:
      - main
jobs:
    Linter_Job:
        runs-on: ubuntu-latest
        steps:
        - name : Instalar dependencias y ejecutar linter
          run: npm install eslint && npm run lint
    
    